import { BodyStyleDirective } from './body-style.directive';

describe('BodyStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new BodyStyleDirective();
    expect(directive).toBeTruthy();
  });
});
